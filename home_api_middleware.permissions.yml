use home_api_middleware:
  title: 'Access HOME API middleware endpoints'
  description: 'Permission to access the HOME API middleware endpoints. Should be permitted for every authenticated user to access accommodation module offers.'